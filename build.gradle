apply plugin: 'java'
apply plugin: 'idea'

repositories {
  mavenCentral()
}

dependencies {
  testCompile 'junit:junit:4.+'
}

sourceSets {
  main {
    java { srcDirs = ['src'] }
  }
  test {
    java { srcDirs = ['test'] }
  }
}
